<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Optimaliseer Ossenhaas Keuzemenu</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <script>
    function handleOptionClick(event) {
      const clickedButton = event.currentTarget;
      const group = clickedButton.closest('.option-group');
      group.querySelectorAll('button').forEach(btn => btn.setAttribute('aria-pressed', 'false'));
      clickedButton.setAttribute('aria-pressed', 'true');
    }
  </script>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen p-4 font-sans">
<div class="w-full max-w-6xl bg-white rounded-xl shadow-lg p-6 md:p-8 space-y-8">
  <h1 class="text-3xl font-bold text-gray-900 text-center">Ossenhaas</h1>

  <div class="border border-gray-200 rounded-lg overflow-hidden">
    <div class="grid grid-cols-1 md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-gray-200">
      <!-- Bijgerecht -->
      <div class="p-4">
        <h3 class="font-semibold text-lg mb-4 text-center text-gray-700">Bijgerecht</h3>
        <div class="space-y-2 option-group">
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-indigo-50 text-indigo-700 font-semibold hover:bg-indigo-100" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Geen</span>
              <span class="text-xs text-gray-500 font-normal">Geen: -</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €0,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
        </div>
      </div>

      <!-- Groente -->
      <div class="p-4">
        <h3 class="font-semibold text-lg mb-4 text-center text-gray-700">Groente</h3>
        <div class="space-y-2 option-group">
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-indigo-50 text-indigo-700 font-semibold hover:bg-indigo-100" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Geen</span>
              <span class="text-xs text-gray-500 font-normal">Geen: -</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €0,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
        </div>
      </div>

      <!-- Saus -->
      <div class="p-4">
        <h3 class="font-semibold text-lg mb-4 text-center text-gray-700">Saus</h3>
        <div class="space-y-2 option-group">
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-indigo-50 text-indigo-700 font-semibold hover:bg-indigo-100" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Geen</span>
              <span class="text-xs text-gray-500 font-normal">Geen: -</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €0,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="true" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
          <button class="w-full flex justify-between items-center text-left p-3 rounded-lg cursor-pointer transition-colors duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-indigo-500 bg-white text-gray-800 hover:bg-gray-50" aria-pressed="false" onclick="handleOptionClick(event)">
            <span>
              <span class="block">Asperges</span>
              <span class="text-xs text-gray-500 font-normal">Allergieën: 🥛</span>
            </span>
            <span class="text-sm font-semibold text-green-600">+ €1,00</span>
          </button>
        </div>
      </div>
    </div>
  </div>

  <div class="flex justify-end pt-4">
    <button class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
      Toevoegen aan bestelling: €...
    </button>
  </div>

  <!-- Besteloverzicht -->
  <section id="besteloverzicht">
    <div class="overflow-x-auto">
      <table class="min-w-full table-auto border border-gray-200 rounded-lg overflow-hidden">
        <thead class="bg-gray-100 text-sm text-gray-600 uppercase text-left">
        <tr>
          <th class="px-4 py-3">Naam</th>
          <th class="px-4 py-3">Categorie</th>
          <th class="px-4 py-3">Bijgerecht</th>
          <th class="px-4 py-3">Groente</th>
          <th class="px-4 py-3">Saus</th>
          <th class="px-4 py-3">Allergieën</th>
          <th class="px-4 py-3 text-right">Actie</th>
          <th class="px-4 py-3 text-right">Prijs</th>
        </tr>
        </thead>
        <tbody id="bestellingTabel" class="text-sm text-gray-700 divide-y divide-gray-200">
        </tbody>
        <tfoot class="bg-gray-50 font-bold">
        <tr class="border-t-2 border-gray-200">
          <td colspan="7" class="px-4 py-4 text-left">Totaal</td>
          <td id="totaalPrijs" class="px-4 py-4 text-right">€0.00</td>
        </tr>
        </tfoot>
      </table>
    </div>
  </section>

  <div class="flex flex-col md:flex-row gap-4 pt-4 border-t border-gray-200">
    <button class="w-full flex items-center justify-center font-bold py-3 px-4 rounded-lg transition duration-300 bg-gray-200 hover:bg-gray-300 text-gray-800">
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
      Terug naar vorige stap
    </button>
    <button class="w-full flex items-center justify-center font-bold py-3 px-4 rounded-lg transition duration-300 bg-gray-800 hover:bg-gray-900 text-white">
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
      </svg>
      Terug naar hoofdmenu
    </button>
  </div>
</div>

<script>
    let bestelLijst = [];

    function tekenBestelTabel() {
        const tbody = document.getElementById("bestellingTabel");
        tbody.innerHTML = "";
        let totaal = 0;

        bestelLijst.forEach((gerecht, index) => {
            totaal += parseFloat(gerecht.prijs);

            const rij = `
                <tr>
                    <td class="px-4 py-3 font-semibold">${gerecht.naam}</td>
                    <td class="px-4 py-3">${gerecht.categorie || '-'}</td>
                    <td class="px-4 py-3">${gerecht.bijgerecht || '-'}</td>
                    <td class="px-4 py-3">${gerecht.groente || '-'}</td>
                    <td class="px-4 py-3">${gerecht.saus || '-'}</td>
                    <td class="px-4 py-3">${(gerecht.allergieen || []).join(" ") || '-'}</td>
                    <td class="px-4 py-3 text-right">
                        <button onclick="verwijderGerecht(${index})" class="text-red-500 hover:text-red-700">
                            <span class="material-icons text-base">delete</span>
                        </button>
                    </td>
                    <td class="px-4 py-3 text-right font-semibold">€${parseFloat(gerecht.prijs).toFixed(2)}</td>
                </tr>`;
            tbody.innerHTML += rij;
        });

        document.getElementById("totaalPrijs").textContent = `€${totaal.toFixed(2)}`;
    }

    function voegToe(id, naam, prijs, categorie, bijgerecht, groente, saus, allergieen) {
        let gerecht = {
            id,
            naam: naam.trim(),
            prijs,
            categorie: categorie?.trim() || '-',
            bijgerecht: bijgerecht || '-',
            groente: groente || '-',
            saus: saus || '-',
            allergieen: allergieen || []
        };

        bestelLijst.push(gerecht);
        localStorage.setItem("winkelmandje", JSON.stringify(bestelLijst));
        tekenBestelTabel();
        document.getElementById("besteloverzicht").scrollIntoView({ behavior: "smooth" });
    }

    function verwijderGerecht(index) {
        bestelLijst.splice(index, 1);
        localStorage.setItem("winkelmandje", JSON.stringify(bestelLijst));
        tekenBestelTabel();
    }

    document.addEventListener("DOMContentLoaded", () => {
        const opgeslagen = localStorage.getItem("winkelmandje");
        if (opgeslagen) {
            bestelLijst = JSON.parse(opgeslagen);
            tekenBestelTabel();
        }
    });
</script>
</body>
</html>
